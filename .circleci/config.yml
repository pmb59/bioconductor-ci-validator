version: 2
jobs:
  build:
    working_directory: /docker-bioconductor
    docker:
      - image: bioconductor/bioconductor_docker:devel
    steps:
      - checkout
      - run:
          name: check installation
          command: R --version
      - run:
          name: build and check ExpressionAtlas
          command: bash build_ExpressionAtlas.sh
